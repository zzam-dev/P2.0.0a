import { MainWrapper } from "@/components/wrappers";
import LoginPage from "@/components/Pages";

const Main = () => {
  return (
    <MainWrapper>
      <LoginPage/>
    </MainWrapper>
  );
};

export default Main;
